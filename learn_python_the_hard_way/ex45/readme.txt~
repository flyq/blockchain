这是一个基于python的，便于扩展的，通过文字与玩家进行交互的，充满趣味的和知识的游戏。

标准：
非无限循环，一律用for；
每一个 if语句，必须包含一个else
如果else永远都不应该执行，必须在else后使用die函数
if语句最多嵌套2层。
布尔测试不超过两个关键字
每个段落都需要分行，每个操作符后都需要空格
代码里每行不超过80个英文字符或者40个汉字，包括格式。

提示输出每行中午最大30个，英文最大60个。
分割线用14个-
